services:
  - type: web
    name: geo-expert-backend
    runtime: python3
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && python server.py"
    envVars:
      - key: GEMINI_API_KEY
        value: "your_gemini_api_key_here"
    plan: free